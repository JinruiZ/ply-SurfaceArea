# ply-SurfaceArea
Calculate the surface area of ply models and plot.

Run AreaCalculator.py to generate a json file, witch containing results. Then run Ploter.py to plot data in the generated json file.
